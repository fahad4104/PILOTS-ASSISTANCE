# 🔍 Debug eCrew Login Issue

## المشكلة الحالية

الخطأ: `Waiting for selector input[type="email"], input[name="username"], input[id="username"]`

هذا يعني أن الـ selectors العامة لا تطابق موقع eCrew الفعلي.

## 🎯 الحل: نحتاج معلومات عن صفحة تسجيل الدخول

### الخيار 1: فحص يدوي (الأفضل)

1. افتح: https://ecrew.etihad.ae/ecrew
2. اضغط `F12` (Developer Tools)
3. اختر تبويب "Elements"
4. انقر على أيقونة المؤشر (Inspect)
5. انقر على حقل البريد الإلكتروني/اسم المستخدم
6. انسخ HTML الذي يظهر

**مثال على ما نحتاجه:**
```html
<input id="username" name="username" type="text" class="form-control" />
```

### الخيار 2: عرض مصدر الصفحة

1. افتح: https://ecrew.etihad.ae/ecrew
2. اضغط `Ctrl+U`
3. ابحث عن "input" أو "username" أو "email"
4. انسخ الأسطر ذات الصلة

### الخيار 3: Screenshot

خذ screenshot لصفحة تسجيل الدخول وأرسلها.

## 🔧 الحلول المحتملة

### إذا كان eCrew يستخدم SSO (Single Sign-On)

مثل Microsoft/Google login:
- **المشكلة**: Puppeteer لن يعمل بسهولة مع SSO
- **الحل**: نحتاج لمعالجة خاصة لـ OAuth/SAML

### إذا كان Form عادي

مع حقول username/password عادية:
- **الحل**: تحديث الـ selectors في الكود

### إذا كانت الصفحة JavaScript-heavy

محملة بالكامل بـ React/Angular:
- **الحل**: إضافة انتظار أطول للعناصر

## 📝 معلومات نحتاجها منك:

يرجى الإجابة على هذه الأسئلة:

1. **نوع تسجيل الدخول:**
   - [ ] Form عادي (username + password)
   - [ ] SSO (Microsoft/Google/Azure AD)
   - [ ] شيء آخر

2. **HTML حقل Username:**
   ```html
   <!-- الصق هنا -->
   ```

3. **HTML حقل Password:**
   ```html
   <!-- الصق هنا -->
   ```

4. **HTML زر Submit:**
   ```html
   <!-- الصق هنا -->
   ```

5. **URL الفعلي لصفحة تسجيل الدخول:**
   ```
   https://ecrew.etihad.ae/ecrew
   أم يُعاد توجيهك لصفحة أخرى؟
   ```

## 🚀 بمجرد حصولنا على المعلومات

سأقوم بـ:
1. تحديث الـ selectors في `src/lib/ecrew-scraper.ts`
2. إضافة معالجة خاصة إذا لزم الأمر
3. اختبار الحل

## 💡 حل مؤقت: استخدام Manual CSV

بينما نحل مشكلة Auto Sync، يمكنك استخدام:
1. اذهب إلى eCrew
2. صدّر الجدول كـ CSV
3. ارفعه في صفحة Import

هذا سيعمل 100% بينما نصلح Auto Sync.

## 📞 تواصل

أرسل لي:
- HTML الأسطر ذات الصلة، أو
- Screenshot لصفحة تسجيل الدخول، أو
- وصف لنوع تسجيل الدخول

وسأحدث الكود فوراً!